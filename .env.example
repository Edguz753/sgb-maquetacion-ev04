# Copia este archivo como .env y ajusta los valores
# Docker Compose
POSTGRES_USER=sgb
POSTGRES_PASSWORD=cambia-esta-clave
POSTGRES_DB=sgb
JWT_SECRET=genera-un-secreto-largo-y-aleatorio

# Backend (backend/.env)
DATABASE_URL=postgresql://sgb:cambia-esta-clave@localhost:5432/sgb?schema=public
JWT_SECRET=genera-un-secreto-largo-y-aleatorio
PORT=3000
